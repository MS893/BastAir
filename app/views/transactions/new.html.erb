<% content_for :title, "Saisir une transaction" %>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Saisir une écriture comptable</h1>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
