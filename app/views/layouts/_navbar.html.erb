<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  
  <div class="container-fluid">

    <!-- Logo BastAir cliquable -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <%= image_tag "logo.png", alt: "BastAir logo", height: "30", class: "me-2" %>
      BastAir
    <% end %>

    <!-- Bouton de navigation pour les petits affichages (hamburger) nécessaire pour l'utilisation du site sur smartphone -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Liens de la barre de navigation -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to t('layouts.navbar.home'), root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
        </li>
        <% unless [:en, :es, :de, :it].include?(I18n.locale) %>
          <li class="nav-item">
            <%= link_to "Tarifs", tarifs_path, class: "nav-link #{'active' if current_page?(tarifs_path)}" %>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <% unless [:en, :es, :de, :it].include?(I18n.locale) %>
            <li class="nav-item">
              <%= link_to "Agenda", agenda_path, class: "nav-link #{'active' if current_page?(agenda_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Évènements", events_path, class: "nav-link #{'active' if current_page?(events_path)}" %>
            </li>
            <!-- MENU DÉROULANT "OUTILS" -->
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownDocs" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                OUTILS
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownDocs">
                <li><%= link_to "Documents divers", documents_divers_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Préparation du vol", documents_divers_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Mes vols, mon carnet de vol", documents_divers_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Formalités DGAC", documents_divers_path, class: "dropdown-item" %></li>
              </ul>
            </li>
            <!-- MENU DÉROULANT "ÉLÈVES" -->
            <% if current_user.eleve? || current_user.admin? || current_user.instructeur? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownEleves" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Élèves
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownEleves">
                  <li><%= link_to "Cours théoriques", elearning_index_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Leçons en vol", flight_lessons_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Livret de progression", livret_progression_path, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% end %>
          <% end %>
        <% else %>
          <li class="nav-item">
            <%= link_to t('layouts.navbar.media_library'), mediatheque_path, class: "nav-link #{'active' if current_page?(mediatheque_path)}" %>
          </li>
        <% end %>
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('layouts.navbar.fleet'), flotte_path, class: "nav-link #{'active' if current_page?(flotte_path)}" %>
          </li>
          <% unless [:en, :es, :de, :it].include?(I18n.locale) %>
          <li class="nav-item">
            <%= link_to "BIA", bia_path, class: "nav-link #{'active' if current_page?(bia_path)}" %>
          </li>
          <% end %>
          <li class="nav-item">
            <%= link_to t('layouts.navbar.discovery_flights'), baptemes_path, class: "nav-link #{'active' if current_page?(baptemes_path)}" %>
          </li>
        <% end %>
        <% unless [:en, :es, :de, :it].include?(I18n.locale) %>
        <li class="nav-item">
          <%= link_to "Calculatrice", outils_path, class: "nav-link #{'active' if current_page?(outils_path)}" %>
        </li>
        <% end %>
        </li>
      </ul>

      <%# Menus déroulants "ADMIN" et "UTILISATEUR" %>
      <ul class="navbar-nav ms-auto">

        <%# Menu de gestion "Admin" visible uniquement pour les administrateurs %>
        <% if user_signed_in? && current_user.admin? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-gear-fill"></i> <%= t('layouts.navbar.admin') %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownAdmin">
              <li><%= link_to admin_news_items_path, class: "dropdown-item" do %><i class="bi bi-exclamation-triangle me-2"></i>Gestion Consignes<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to transactions_path, class: "dropdown-item" do %><i class="bi bi-journal-text me-2"></i>Afficher les comptes<% end %></li>
              <li><%= link_to new_transaction_path, class: "dropdown-item" do %><i class="bi bi-plus-circle me-2"></i>Saisir une transaction<% end %></li>
              <li><%= link_to analytics_transactions_path, class: "dropdown-item" do %><i class="bi bi-bar-chart-line-fill me-2"></i>Analyse Financière<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to users_path, class: "dropdown-item" do %><i class="bi bi-person me-2"></i>Liste des adhérents<% end %></li>
              <li><%= link_to new_admin_user_path, class: "dropdown-item" do %><i class="bi bi-person-plus me-2"></i>Créer un nouvel adhérent<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to vols_path, class: "dropdown-item" do %><i class="bi bi-airplane me-2"></i>Afficher les vols<% end %></li>
              <li><%= link_to admin_reservations_path, class: "dropdown-item" do %><i class="bi bi-calendar-check me-2"></i>Gestion des Réservations<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to admin_maintenances_path, class: "dropdown-item" do %><i class="bi bi-wrench-adjustable me-2"></i>Maintenance avion(s)<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to admin_tables_path, class: "dropdown-item" do %><i class="bi bi-server me-2"></i>Gérer les tables (BDD)<% end %></li>
              <li><%= link_to admin_tables_path(table_name: 'activity_logs'), class: "dropdown-item" do %><i class="bi bi-clock-history me-2"></i>Historique d'activité<% end %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to admin_google_calendars_path, class: "dropdown-item" do %><i class="bi bi-exclamation-square me-2"></i>Effacer les agendas Google (IRREVERSIBLE)<% end %></li>
              <li><%= link_to edit_admin_setting_path, class: "dropdown-item" do %><i class="bi bi-clock me-2"></i>Modifier le fuseau horaire<% end %></li>
            </ul>
          </li>
        <% end %>

        <%# Menu utilisateur (connexion/déconnexion, profil, etc.) %>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li><%= link_to t('layouts.navbar.profile'), user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to t('layouts.navbar.edit_profile'), edit_profil_user_path, class: "dropdown-item" %></li>
              <li><%= link_to t('layouts.navbar.edit_password'), edit_user_registration_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%# Utilisation de button_to pour la déconnexion (pas link_to), ce qui génère un formulaire avec la méthode DELETE (voir stackoverflow) %>
                <%= button_to t('layouts.navbar.logout'), destroy_user_session_path, method: :delete, class: "dropdown-item", form_class: "d-inline" %>
              </li>
            </ul>
          </li>
        <% else %>
          <% unless [:en, :es, :de, :it].include?(I18n.locale) %>
          <li class="nav-item">
            <%= link_to t('layouts.navbar.member_area'), new_user_session_path, class: "nav-link" %>
          </li>
          <% end %>
        <% end %>

      </ul>

      <%# Sélecteur de langue et de changement de thème %>
      <div class="d-flex align-items-center ms-3">

        <%# Sélecteur de langue %>
        <% unless user_signed_in? %>
        <div class="dropdown me-3">
          <% flag_codes = { fr: "fr", en: "gb", es: "es", de: "de", it: "it" } %>
          <a class="text-decoration-none text-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= image_tag "flags/#{flag_codes[I18n.locale.to_sym]}.png", width: 20, height: 15, alt: I18n.locale.to_s, class: "me-1 flag-icon" %> <%= I18n.locale.to_s.upcase %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" style="min-width: auto;">
            <% flag_codes.each do |loc, code| %>
              <li>
                <%= link_to url_for(request.params.merge(locale: loc)), data: { turbo: false }, class: "dropdown-item #{'active' if I18n.locale == loc}" do %>
                  <%= image_tag "flags/#{code}.png", width: 20, height: 15, alt: loc.to_s, class: "me-2 flag-icon" %> <%= loc.to_s.upcase %>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
        <% end %>

        <%# Bouton de changement de thème (clair/sombre) %>
        <i class="bi bi-sun-fill text-light me-2"></i>
        <div class="form-check form-switch">
          <input class="form-check-input" type="checkbox" role="switch" id="theme-toggle">
        </div>
        <i class="bi bi-moon-fill text-light ms-1"></i>
      </div>

    </div>
  </div>
  
</nav>