<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document PDF</title>

  <%# Chargement de Bootstrap via CDN (requis pour le rendu des styles comme les cards, tables, etc.) %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <%# feuille de style spécifique PDF %>
  <%#= wicked_pdf_stylesheet_link_tag "pdf" -%>

  <style>
    body {
      font-family: sans-serif;
      font-size: 12px;
      background-color: #fff;
    }
    
    /* Force l'impression des couleurs de fond (important pour les badges et headers de cards) */
    * {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    /* Évite les coupures au milieu des tableaux ou des cartes */
    .card, table, tr, td, th {
      page-break-inside: avoid;
    }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
